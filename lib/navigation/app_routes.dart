part of 'app_router.dart';

///For routes use the names use route-name naming convention(dash-case)
///For normal routes use /route-name
///For sub routes use route-name
class AppRoutes {
  static const splash = "/";
  static const login = "/login";
  static const dashboard = "/dashboard";
}
